let hello = "Hello";
let world = "World";
let epicPhrase = hello + ' ' + world + '!';
//console.log(epicPhrase);

// Template literals

let epicPhrase2 = `${hello} ${world}!`;
console.log(epicPhrase2);


// Multi-line strings

let lores = "esto es un string \n " + "esto es otra linea";

let lores2 = `esto es una frase epica
    la continuacion de esa frase epica
`;

console.log(lores);
console.log(lores2);